/** StudyHeatmap — activity heatmap stub (future feature). */
import React from "react";

export default function StudyHeatmap() {
    return (
        <div className="card" style={{ padding: "1.25rem" }}>
            <div style={{ fontWeight: 700, color: "var(--navy)", marginBottom: ".75rem" }}>🔥 Study Heatmap</div>
            <div style={{ fontSize: ".85rem", color: "var(--text-muted)", textAlign: "center", padding: "1.5rem 0" }}>
                Daily activity data will appear here after you complete quizzes.
            </div>
        </div>
    );
}
